curl -sf https://binaries.nats.dev/nats-io/nats-server/v2@latest | sh

./nats-server -js
nats req one hello2 --wait
nats str ls

./nats-server -js
nats str add one --subjects "one" --defaults
nats str info one -j | jq > one.config
nats str add one --config one.config
./nats-server -c server.config

#sudo snap install mqttx
npm install mqttx-cli -g
mqttx pub -t one -m hello9 -V 3.1.1

nats str purge one --subject one

websocat ws://localhost:4195/get/ws | xxd
ffmpeg -i out.mp4 -vf "scale=1920:1080:flags=neighbor" -c:v libx264 -pix_fmt yuv420p -r 60 -vsync cfr out_1080p60.mp4
./start_telephone_game.sh
uv run pbm_stream_to_ffmpeg.py out.mp4
