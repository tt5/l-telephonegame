[project]
name = "nats"
version = "0.1.0"
description = "Telephone game video pipeline — NATS pub/sub with CVAE digit generation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nats-py>=2.15.0",
    "numpy>=2.4.6",
    "onnxruntime>=1.26.0",
    "opencv-python>=4.10.0",
    "websockets>=16.0",
]
